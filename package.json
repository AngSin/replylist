{
  "name": "whitelist-tool",
  "version": "1.0.0",
  "scripts": {
    "root": "npx ts-node scripts/generateRoot.ts",
    "json": "npx ts-node scripts/generateJSON.ts",
    "generate": "npx ts-node scripts/downloadAddresses.ts && npm run root && npm run json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.264.0",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@openzeppelin/merkle-tree": "^1.0.2",
    "prettier": "^2.8.3"
  }
}
